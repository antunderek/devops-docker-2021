version: '3.5'

services:

        backend:
                image: backend-go
                ports: 
                        - 8080:8080

        frontend:
                image: hello-frontend
                ports: 
                        - 5000:5000
